import React from 'react';
import s from "./AddTest.module.css"

const AddTest = () => {
    return (
        <div className={s.addTest}>
            <div className={`__container ${s.addTest__container}`}>

            </div>
        </div>
    );
};

export default AddTest;